<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{% block title %}Bootstrap from twitter!{% endblock %}</title>
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->

    <!-- Le styles -->
    <link rel="stylesheet" href="http://twitter.github.com/bootstrap/1.3.0/bootstrap.min.css">
    <style type="text/css">
      /* Override some defaults */
      html, body {
        background-color: #eee;
      }
      body {
        padding-top: 40px; /* 40px to make the container go all the way to the bottom of the topbar */
      }
      .container > footer p {
        text-align: center; /* center align it with the container */
      }

      /* The white background content wrapper */
      .content {
        background-color: #fff;
        padding: 20px;
        margin: 0 -20px; /* negative indent the amount of the padding to maintain the grid system */
        -webkit-border-radius: 0 0 6px 6px;
           -moz-border-radius: 0 0 6px 6px;
                border-radius: 0 0 6px 6px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.15);
           -moz-box-shadow: 0 1px 2px rgba(0,0,0,.15);
                box-shadow: 0 1px 2px rgba(0,0,0,.15);
      }

      /* Page header tweaks */
      .page-header {
        background-color: #f5f5f5;
        padding: 20px 20px 10px;
        margin: -20px -20px 20px;
      }

      /* Styles you shouldn't keep as they are for displaying this base example only */
      .content .span16 {
        min-height: 500px;
      }
      /* Give a quick and non-cross-browser friendly divider */
      .content .span4 {
        margin-left: 0;
        padding-left: 19px;
        border-left: 1px solid #eee;
      }

      .topbar .btn {
        border: 0;
      }

    </style>

    <!-- Le fav and touch icons -->
    <link rel="shortcut icon" href="{{ asset('favicon.ico') }}">
    <link rel="apple-touch-icon" href="{{ asset('apple-touch-icon.png') }}">
  </head>

  <body>

    <div class="topbar">
      <div class="fill">
        <div class="container">
          <a class="brand" href="#">Opensoft Code Conversations</a>
          <ul class="nav">
            <li class="active"><a href="{{ path('opensoft_gversation_default_homepage')}}">Home</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#contact">Contact</a></li>
          </ul>

          {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
              <ul class="nav">
                <li>{{ app.user.username }}</li>
                <li><a href="{{ path('fos_user_security_logout') }}" class="btn">Sign out</a></li>
              </ul>
          {% else %}
            <form action="{{ path("fos_user_security_check") }}" method="post" class="pull-right">
              <input class="input-small" id="username" name="_username" type="text" placeholder="Username">
              <input class="input-small" id="password" name="_password" type="password" placeholder="Password">
              <button class="btn" type="submit">Sign in</button>
            </form>
          {% endif %}
        </div>
      </div>
    </div>

    <div class="container">

      <div class="content">
        <div class="page-header">
          <h1>{% block page_title %}Page name{% endblock %} <small>{% block page_subtitle %}Supporting text or tagline{% endblock %}</small></h1>
        </div>
        <div class="row">
            <div class="span16">
                {% block body %}{% endblock %}
            </div>
        </div>
      </div>

      <footer>
        <p>&copy; Opensoft 2011</p>
      </footer>

    </div> <!-- /container -->

    {% block javascripts %}{% endblock %}

  </body>
</html>